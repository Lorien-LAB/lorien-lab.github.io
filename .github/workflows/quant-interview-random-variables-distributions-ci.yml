name: Quant Interview Random Variables Distributions CI

on:
  push:
    branches:
      - chatgpt/quant-interview-workstream-random-variables-distributions-2026-08-18
  workflow_dispatch:

permissions:
  contents: write

jobs:
  verify-red-knowledge-advanced:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - name: Verify Gaussian and limit theorem RED
        run: |
          set +e
          npm run test > /tmp/rvd-red.log 2>&1
          status=$?
          set -e
          cat /tmp/rvd-red.log
          test "$status" -ne 0
          grep -Eqi "gaussian-lognormal-structure.md|limit-theorems-lln-clt.md" /tmp/rvd-red.log
          grep -Eqi "ENOENT|no such file" /tmp/rvd-red.log
      - name: Record expected RED
        run: |
          mkdir -p .ci
          {
            printf 'commit=%s\n' "$GITHUB_SHA"
            printf 'run_id=%s\n' "$GITHUB_RUN_ID"
            printf 'expected_failure=missing-gaussian-limit-knowledge\n'
          } > .ci/rvd-red-knowledge-advanced.txt
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .ci/rvd-red-knowledge-advanced.txt
          git commit -m "chore: record RVD Gaussian limit Knowledge RED"
          git push
